There are lot of issues on the web app that needs to be fixed:

1. Create a custom ResponsiveDialog component that alternates between using a Dialog component on large screens and a Drawer on mobile screens for modals.
2. Clicking on the logout icon redirects me the api route instead of showing a modal confirming the action and redirecting me to the login page.
3. The navbar is broken on mobile screens, the sidebar and logo button is in the same horizontal position as the body content.
4. The select and input component on the page is using the native select element instead of shadcn ui components.
